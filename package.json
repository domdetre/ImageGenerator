{
  "name": "image-generator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest"
    "start": "source alias.sh && dc up -d",
    "stop": "source alias.sh && dc down -v --remove-orphans"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^26.4.2",
    "serverless": "^2.3.0"
  }
}
